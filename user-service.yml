server:
  port: 8081
security:
  oauth2:
    resource:
      token-info-uri: http://localhost:9000/oauth/check_token/
spring:
  application:
    name: user-service
  zipkin:
#    enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka
